{
  "name": "mps3",
  "description": "Provide clientside multiplayer and optimistic updates over any s3-compatible storage API",
  "version": "0.0.42",
  "license": "MIT",
  "author": {
    "name": "Tom Larkworthy"
  },
  "main": "dist/mps3.js",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun build src/mps3.ts --minify --outdir ./dist",
    "prepare": "npm run build",
    "format": "rome format src --write --indent-style space",
    "test:randomize": "while bun test; [ $? -ne 1 ] ; do :; done"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "3.370.0",
    "bun": "^0.7.0",
    "bun-types": "^0.7.0",
    "rome": "^12.1.3"
  },
  "dependencies": {
    "json-merge-patch": "^1.0.2"
  }
}
